<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ============================================== -->
    <!-- TreeView Style -->
    <!-- ============================================== -->
    <Style TargetType="TreeView">
        <Setter Property="Background" Value="{ThemeResource SurfaceBackground}" />
        <Setter Property="BorderBrush" Value="{ThemeResource ControlBorder}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="12" />
        <Setter Property="Template">
            <ControlTemplate TargetType="TreeView">
                <Grid>
                    <Border Name="RootBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{TemplateBinding CornerRadius}"
                            Shape="RoundedRectangle"
                            ClipToBounds="True">
                        <ScrollViewer Name="PART_ScrollViewer"
                                      HorizontalScrollBarVisibility="Auto"
                                      VerticalScrollBarVisibility="Auto">
                            <ItemsPresenter />
                        </ScrollViewer>
                    </Border>
                </Grid>
                <ControlTemplate.Triggers>
                    <Trigger Property="IsKeyboardFocused" Value="True">
                        <Setter TargetName="RootBorder" Property="BorderBrush" Value="{ThemeResource ControlBorderFocused}" />
                    </Trigger>
                </ControlTemplate.Triggers>
            </ControlTemplate>
        </Setter>

        <Style.Triggers>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Foreground" Value="{ThemeResource TextDisabled}" />
                <Setter Property="Opacity" Value="0.56" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ============================================== -->
    <!-- TreeSelector Style (drop-down with tree popup) -->
    <!-- ============================================== -->
    <Style TargetType="TreeSelector">
        <Setter Property="Background" Value="{ThemeResource ControlBackground}" />
        <Setter Property="BorderBrush" Value="{ThemeResource ControlBorder}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="10,6,6,6" />
        <Setter Property="MinHeight" Value="36" />
        <Setter Property="CornerRadius" Value="10" />
        <Setter Property="Template">
            <ControlTemplate TargetType="TreeSelector">
                <Grid>
                    <!-- Trigger row -->
                    <Border Name="PART_TriggerBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{TemplateBinding CornerRadius}"
                            Shape="RoundedRectangle"
                            Padding="{TemplateBinding Padding}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <!-- Content area (column 0): chips / display text / search box / placeholder, layered. -->
                            <Grid Grid.Column="0" VerticalAlignment="Center">
                                <!-- Multi-select chip list -->
                                <WrapPanel Name="PART_TagsPanel"
                                           Orientation="Horizontal"
                                           VerticalAlignment="Center"
                                           Visibility="Collapsed" />

                                <!-- Single-select path display -->
                                <TextBlock Name="PART_DisplayText"
                                           VerticalAlignment="Center"
                                           Visibility="Collapsed"
                                           TextTrimming="CharacterEllipsis"
                                           TextWrapping="NoWrap" />

                                <!-- Optional search input -->
                                <TextBox Name="PART_SearchTextBox"
                                         BorderThickness="0"
                                         Background="Transparent"
                                         Padding="0"
                                         MinHeight="0"
                                         VerticalAlignment="Center"
                                         Visibility="Collapsed" />

                                <!-- Placeholder when nothing selected and no search text -->
                                <TextBlock Name="PART_PlaceholderText"
                                           Text="{TemplateBinding PlaceholderText}"
                                           VerticalAlignment="Center"
                                           Foreground="{ThemeResource TextSecondary}"
                                           IsHitTestVisible="False" />
                            </Grid>

                            <!-- Clear button (column 1) -->
                            <Button Name="PART_ClearButton"
                                    Grid.Column="1"
                                    Width="20"
                                    Height="20"
                                    Margin="4,0,0,0"
                                    Padding="0"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    VerticalAlignment="Center"
                                    Visibility="Collapsed">
                                <Path Data="M 0,0 L 8,8 M 8,0 L 0,8"
                                      Stroke="{ThemeResource TextSecondary}"
                                      StrokeThickness="1.4"
                                      Width="8"
                                      Height="8"
                                      Stretch="Uniform"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center" />
                            </Button>

                            <!-- Drop-down arrow (column 2) -->
                            <Path Name="PART_DropDownArrow"
                                  Grid.Column="2"
                                  Data="M 0,0 L 8,0 L 4,5 Z"
                                  Fill="{ThemeResource TextSecondary}"
                                  Width="10"
                                  Height="6"
                                  Stretch="Uniform"
                                  Margin="6,0,4,0"
                                  VerticalAlignment="Center" />
                        </Grid>
                    </Border>

                    <!-- Popup containing the tree -->
                    <Popup Name="PART_Popup"
                           IsOpen="{TemplateBinding IsDropDownOpen}"
                           StaysOpen="False"
                           Placement="Bottom"
                           PlacementTarget="{Binding RelativeSource={RelativeSource AncestorType=TreeSelector}}">
                        <Grid MinWidth="240">
                            <Rectangle Fill="{ThemeResource MenuFlyoutPresenterBackground}"
                                       Stroke="{ThemeResource MenuFlyoutPresenterBorderBrush}"
                                       StrokeThickness="1"
                                       RadiusX="14"
                                       RadiusY="14" />
                            <ScrollViewer Margin="6"
                                          MaxHeight="{Binding MaxDropDownHeight, RelativeSource={RelativeSource AncestorType=TreeSelector}}"
                                          HorizontalScrollBarVisibility="Auto"
                                          VerticalScrollBarVisibility="Auto">
                                <ItemsPresenter />
                            </ScrollViewer>
                        </Grid>
                    </Popup>
                </Grid>
                <ControlTemplate.Triggers>
                    <Trigger SourceName="PART_TriggerBorder" Property="IsMouseOver" Value="True">
                        <Setter TargetName="PART_TriggerBorder" Property="Background" Value="{ThemeResource ControlBackgroundHover}" />
                        <Setter TargetName="PART_TriggerBorder" Property="BorderBrush" Value="{ThemeResource ControlBorderHover}" />
                    </Trigger>
                    <Trigger Property="IsKeyboardFocused" Value="True">
                        <Setter TargetName="PART_TriggerBorder" Property="BorderBrush" Value="{ThemeResource ControlBorderFocused}" />
                    </Trigger>
                    <Trigger Property="IsEnabled" Value="False">
                        <Setter TargetName="PART_TriggerBorder" Property="Background" Value="{ThemeResource ControlBackgroundDisabled}" />
                        <Setter TargetName="PART_TriggerBorder" Property="BorderBrush" Value="{ThemeResource ControlBorderDisabled}" />
                    </Trigger>
                </ControlTemplate.Triggers>
            </ControlTemplate>
        </Setter>

        <Style.Triggers>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Foreground" Value="{ThemeResource TextDisabled}" />
                <Setter Property="Opacity" Value="0.56" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ============================================== -->
    <!-- TreeSelectorItem Style -->
    <!-- ============================================== -->
    <Style TargetType="TreeSelectorItem">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{ThemeResource TextPrimary}" />
        <Setter Property="Padding" Value="6,3,6,3" />
        <Setter Property="CornerRadius" Value="8" />
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="Template">
            <ControlTemplate TargetType="TreeSelectorItem">
                <StackPanel>
                    <!-- Header Row -->
                    <Border Name="PART_HeaderBorder"
                            Background="{TemplateBinding Background}"
                            CornerRadius="{TemplateBinding CornerRadius}"
                            Shape="RoundedRectangle"
                            Height="28">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <!-- Indent Spacer -->
                            <Grid Name="PART_IndentSpacer"
                                  Grid.Column="0"
                                  Width="0" />

                            <!-- Expander Arrow — 圆角朝下三角形（用户指定 SVG path,
                                 几何在 ~1160×1012 bbox），Stretch=Uniform 缩放进
                                 8×8 槽。SetExpanderAngle 用绝对像素 CenterX/CenterY
                                 锚定旋转中心,避免 RenderTransformOrigin 相对单位 +
                                 Stretch 缩放共同导致的锚点偏移。自然朝下 = 展开态,
                                 折叠态由代码旋转 -90° 改为朝右。 -->
                            <Grid Name="PART_ExpanderBorder"
                                  Grid.Column="1"
                                  Width="16"
                                  Visibility="Collapsed">
                                <Path Name="PART_ExpanderArrow"
                                      Data="M 733.87,841.90 L 1160.54,273.07 A 170.67,170.67,0,0,0,1024.00,0 H 170.67 A 170.67,170.67,0,0,0,34.14,273.07 L 460.80,841.90 A 170.67,170.67,0,0,0,733.87,841.90 Z"
                                      Fill="{TemplateBinding Foreground}"
                                      Width="8"
                                      Height="8"
                                      Stretch="Uniform"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center" />
                            </Grid>

                            <!-- Optional Check Box (visibility driven by TreeSelector.ShowCheckBoxes) -->
                            <CheckBox Name="PART_CheckBox"
                                      Grid.Column="2"
                                      Margin="0,0,4,0"
                                      VerticalAlignment="Center"
                                      Visibility="Collapsed" />

                            <!-- Header Content -->
                            <ContentPresenter Name="PART_Header"
                                              Grid.Column="3"
                                              Content="{TemplateBinding Header}"
                                              ContentTemplate="{TemplateBinding HeaderTemplate}"
                                              HorizontalAlignment="Left"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}" />
                        </Grid>
                    </Border>

                    <!-- Children Panel -->
                    <ItemsPresenter Name="PART_ItemsHost"
                                    Visibility="Collapsed" />
                </StackPanel>
                <ControlTemplate.Triggers>
                    <Trigger SourceName="PART_HeaderBorder" Property="IsMouseOver" Value="True">
                        <Setter TargetName="PART_HeaderBorder" Property="Background" Value="{ThemeResource ControlBackgroundHover}" />
                    </Trigger>
                    <Trigger Property="IsSelected" Value="True">
                        <Setter TargetName="PART_HeaderBorder" Property="Background" Value="{ThemeResource SelectionBackground}" />
                    </Trigger>
                    <MultiTrigger>
                        <MultiTrigger.Conditions>
                            <Condition Property="IsSelected" Value="True" />
                            <Condition SourceName="PART_HeaderBorder" Property="IsMouseOver" Value="True" />
                        </MultiTrigger.Conditions>
                        <Setter TargetName="PART_HeaderBorder" Property="Background" Value="{ThemeResource AccentBrushPressed}" />
                    </MultiTrigger>
                </ControlTemplate.Triggers>
            </ControlTemplate>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Foreground" Value="{ThemeResource TextDisabled}" />
                <Setter Property="Opacity" Value="0.56" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ============================================== -->
    <!-- TreeViewItem Style -->
    <!-- ============================================== -->
    <Style TargetType="TreeViewItem">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{ThemeResource TextPrimary}" />
        <Setter Property="Padding" Value="6,3,6,3" />
        <Setter Property="CornerRadius" Value="8" />
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="Template">
            <ControlTemplate TargetType="TreeViewItem">
                <StackPanel>
                    <!-- Header Row -->
                    <Border Name="PART_HeaderBorder"
                            Background="{TemplateBinding Background}"
                            CornerRadius="{TemplateBinding CornerRadius}"
                            Shape="RoundedRectangle"
                            Height="28">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <!-- Indent Spacer -->
                            <Grid Name="PART_IndentSpacer"
                                  Grid.Column="0"
                                  Width="0" />

                            <!-- Expander Arrow — 圆角朝下三角形（用户指定 SVG path）,
                                 Stretch=Uniform 缩放进 8×8 槽。自然朝下 = 展开态,
                                 折叠态由代码旋转 -90° 改为朝右。 -->
                            <Grid Name="PART_ExpanderBorder"
                                  Grid.Column="1"
                                  Width="16"
                                  Visibility="Collapsed">
                                <Path Name="PART_ExpanderArrow"
                                      Data="M 733.87,841.90 L 1160.54,273.07 A 170.67,170.67,0,0,0,1024.00,0 H 170.67 A 170.67,170.67,0,0,0,34.14,273.07 L 460.80,841.90 A 170.67,170.67,0,0,0,733.87,841.90 Z"
                                      Fill="{TemplateBinding Foreground}"
                                      Width="8"
                                      Height="8"
                                      Stretch="Uniform"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center" />
                            </Grid>

                            <!-- Header Content -->
                            <ContentPresenter Name="PART_Header"
                                              Grid.Column="2"
                                              Content="{TemplateBinding Header}"
                                              ContentTemplate="{TemplateBinding HeaderTemplate}"
                                              HorizontalAlignment="Left"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}" />
                        </Grid>
                    </Border>

                    <!-- Children Panel — use ItemsPresenter like WPF so the standard
                         ItemsControl pipeline manages child containers. -->
                    <ItemsPresenter Name="PART_ItemsHost"
                                    Visibility="Collapsed" />
                </StackPanel>
                <ControlTemplate.Triggers>
                    <Trigger SourceName="PART_HeaderBorder" Property="IsMouseOver" Value="True">
                        <Setter TargetName="PART_HeaderBorder" Property="Background" Value="{ThemeResource ControlBackgroundHover}" />
                    </Trigger>
                    <Trigger Property="IsSelected" Value="True">
                        <Setter TargetName="PART_HeaderBorder" Property="Background" Value="{ThemeResource SelectionBackground}" />
                    </Trigger>
                    <MultiTrigger>
                        <MultiTrigger.Conditions>
                            <Condition Property="IsSelected" Value="True" />
                            <Condition SourceName="PART_HeaderBorder" Property="IsMouseOver" Value="True" />
                        </MultiTrigger.Conditions>
                        <Setter TargetName="PART_HeaderBorder" Property="Background" Value="{ThemeResource AccentBrushPressed}" />
                    </MultiTrigger>
                </ControlTemplate.Triggers>
            </ControlTemplate>
        </Setter>

        <Style.Triggers>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Foreground" Value="{ThemeResource TextDisabled}" />
                <Setter Property="Opacity" Value="0.56" />
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>
